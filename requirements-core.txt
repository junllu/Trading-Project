# Essentials to run the portal (dashboard + daily agent). These are all
# pure-Python or ship prebuilt wheels, so they install quickly on every OS.
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.9
pydantic>=2.6
PyYAML>=6.0
python-dotenv>=1.0
cryptography>=42.0
requests>=2.31
anthropic>=0.40
