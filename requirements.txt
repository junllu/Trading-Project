# Full install = essentials + broker integrations.
# The launchers install requirements-core.txt first (so the app always runs),
# then attempt the broker libraries below (needed only for LIVE Robinhood/Webull).
-r requirements-core.txt

# Broker integrations (unofficial — only needed when you go live on a real broker)
robin-stocks>=3.1
webull>=0.6
